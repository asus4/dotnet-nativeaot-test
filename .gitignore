# .NET
[Bb]in/
[Oo]bj/

# Build artifacts
*.xcframework
*.aar
*.so

# OpenSSL cross-build (cloned source + per-ABI build trees) for Android HTTPS
build/openssl-src/
build/openssl-build/

# NativeFib sample package build outputs (regenerate via ./build/build_nativefib.sh)
packages/NativeFib/runtimes/
packages/local-feed/

## Xcode user data
xcuserdata/
